import { mockPinnedProjectsData } from "@/mocks/pinned-projects.mock";
import { projectComments } from "@/mocks/project-comments.mock";
import { projectStatuses } from "@/mocks/project-statuses.mock";
import { projects } from "@/mocks/projects.mock";
import { taskComments } from "@/mocks/task-comments";
import { taskStatuses } from "@/mocks/task-statuses.mock";
import { tasks } from "@/mocks/tasks.mock";

export {
  mockPinnedProjectsData,
  taskStatuses,
  projectStatuses,
  taskComments,
  projectComments,
  tasks,
  projects,
};
